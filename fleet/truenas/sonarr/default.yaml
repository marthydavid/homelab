ingress:
  main:
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    enabled: true
    hosts:
    - host: sonarr.tl.dr
      paths:
      - path: /
        pathType: Prefix
    ingressClassName: nginx
    tls:
    - hosts:
      - sonarr.tl.dr
      secretName: sonarr-tls
podSecurityContext:
  runAsUser: 568
  runAsGroup: 568
  fsGroup: 568
persistence:
  config:
    enabled: true
    type: pvc
    storageClass: ix-storage-class-sonarr
  media:
    enabled: true
    type: hostPath
    hostPath: /zfs/media/series
    mountPath: /media
metrics:
  enabled: true
  prometheusRule:
    enabled: true
  exporter:
    env:
      unknownQueueItems: true