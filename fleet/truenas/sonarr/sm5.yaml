ingress:
  main:
    annotationsList:
    - name: cert-manager.io/cluster-issuer
      value: letsencrypt-prod
    enableFixedMiddlewares: true
    enabled: true
    entrypoint: websecure
    expert: true
    hosts:
    - host: sonarr.budapest.marthy.xyz
      paths:
      - path: /
        pathType: Prefix
    ingressClassName: ""
    labelsList: []
    middlewares: []
    tls:
    - hosts:
      - sonarr.budapest.marthy.xyz
      secretName: sonarr-tls
persistenceList:
- advanced: false
  enabled: true
  hostPathSimple: /mnt/hdd/plex/series
  mountPath: /mnt/hdd/plex/series
  name: series
  readOnly: false
  setPermissionsSimple: false
  type: simpleHP