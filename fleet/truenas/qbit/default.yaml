ingress:
  main:
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    enabled: true
    hosts:
    - host: qbit.tl.dr
      paths:
      - path: /
        pathType: Prefix
    ingressClassName: nginx
    tls:
    - hosts:
      - qbit.tl.dr
      secretName: qbit-tls
podSecurityContext:
  runAsUser: 568
  runAsGroup: 568
  fsGroup: 568
persistence:
  config:
    enabled: true
    type: pvc
    storageClass: ix-storage-class-qbit
  media:
    enabled: true
    type: hostPath
    hostPath: /zfs/media
    mountPath: /media
  downloads:
    enabled: true
    type: hostPath
    hostPath: /zfs/media
    mountPath: /downloads
metrics:
  enabled: true
  prometheusRule:
    enabled: true
  exporter:
    env:
      unknownQueueItems: true