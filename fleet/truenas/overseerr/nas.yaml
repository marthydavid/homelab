ingress:
  main:
    hosts:
    - host: overseerr.pecs.marthy.xyz
      paths:
      - path: /
        pathType: Prefix
    tls:
    - hosts:
      - overseerr.pecs.marthy.xyz
      secretName: overseerr-tls
