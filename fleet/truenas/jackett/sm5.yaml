ingress:
  main:
    annotationsList:
    - name: cert-manager.io/cluster-issuer
      value: letsencrypt-prod
    enableFixedMiddlewares: true
    enabled: true
    entrypoint: websecure
    expert: true
    hosts:
    - host: jackett.budapest.marthy.xyz
      paths:
      - path: /
        pathType: Prefix
    ingressClassName: ""
    labelsList: []
    middlewares: []
    tls:
    - hosts:
      - jackett.budapest.marthy.xyz
      secretName: jackett-tls