ingress:
  main:
    annotationsList:
    - name: cert-manager.io/cluster-issuer
      value: letsencrypt-prod
    enableFixedMiddlewares: true
    enabled: true
    entrypoint: websecure
    expert: true
    hosts:
    - host: radarr.budapest.marthy.xyz
      paths:
      - path: /
        pathType: Prefix
    ingressClassName: ""
    labelsList: []
    middlewares: []
    tls:
    - hosts:
      - radarr.budapest.marthy.xyz
      secretName: radarr-tls
persistenceList:
- advanced: false
  enabled: true
  hostPathSimple: /mnt/hdd/plex/movies
  mountPath: /mnt/hdd/plex/movies
  name: movies
  readOnly: false
  setPermissionsSimple: false
  type: simpleHP